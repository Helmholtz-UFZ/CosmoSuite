"""CSV statistical profiler — the reference domain for the Cosmo Suite.

Depends on the ``cosmo_suite`` package and plugs the CSV profiler into it by
injecting the Job seams (config model, file validator, submit handler) at startup.
"""
