"""CSV statistical profiler — the reference domain for the Cosmo Framework.

Depends on the ``cosmo_framework`` package and plugs the CSV profiler into it by
injecting the Job seams (config model, file validator, submit handler) at startup.
"""
